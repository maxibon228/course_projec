# TEST_REPORT — Cipher Web 4.4

Дата проверки: 2026-06-08

## Выполненные команды

```bash
python -m py_compile cypher.py srv.py admin.py
pytest -q
```

## Результат

```text
...........                                                              [100%]
11 passed in 0.38s
```

## Что проверено автоматическими тестами

1. Все зарегистрированные алгоритмы проходят round-trip: шифрование → расшифровка.
2. В реестре есть `caesar`, `atbash`, `vigenere`.
3. Неизвестный алгоритм безопасно заменяется на `caesar`.
4. Маршрут `/` отдаёт redirect на основную страницу.
5. Главная страница `/70193935` открывается без ошибки.
6. В HTML есть `Cipher Web`, `Шифрование сообщений`, кнопка sidebar toggle и блок scroll-статистики.
7. В HTML нет лишних элементов: `Студенческий проект`, `v4.2 · Атбаш · светлая тема`, `Настройки`.
8. POST-сохранение работает для `caesar`, `atbash`, `vigenere`.
9. Записи корректно сохраняются в CSV и читаются обратно с расшифровкой.
10. Статистика строится динамически по всем алгоритмам.
11. `/get_all.json` возвращает расшифрованные сообщения.
12. Старый CSV-формат из 4 полей продолжает поддерживаться.
13. Страница пасхалки содержит ссылку `Перейти на следующий этап квеста` на `https://t.me/TeaQuizzesBot`.
14. Старый текст пасхалки `Чай сохранён для перерыва между шифрами.` отсутствует.
15. CSS содержит flex-ограничение scroll-окна алгоритмов: две карточки занимают всю видимую высоту блока, остальные прокручиваются.

## Ручная проверка в браузере

После запуска `python srv.py` открыть:

```text
http://127.0.0.1:5000/70193935
```

Проверить:

- кнопка-бургер сворачивает и разворачивает левую панель;
- состояние sidebar сохраняется после перезагрузки страницы;
- `Всего сообщений` осталось в верхней левой карточке блока статистики;
- `Добавлено админом` находится слева;
- `Цезарь`, `Атбаш`, `Виженер` находятся справа в вертикальном scroll-блоке;
- пункт меню `Настройки` отсутствует;
- новая запись с шифром `Виженер` сохраняется и отображается расшифрованной.


## Дополнительные проверки версии 4.4

- Проверена новая ссылка пасхалки `Перейти на следующий этап квеста` на `https://t.me/TeaQuizzesBot`.
- Проверено, что старый текст `Чай сохранён для перерыва между шифрами.` больше не отображается.
- Проверено наличие CSS-переменной `--algorithm-card-gap`, stretch-растяжения правого блока и правила `flex: 0 0 calc(50% - 7px)` для отображения двух карточек шифров в видимой области.
